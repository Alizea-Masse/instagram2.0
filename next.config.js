/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.meilleure-innovation.com', 'webdrop.fr', 'cloudflare-ipfs.com'],
  },
}

module.exports = nextConfig
